{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx convex deploy --cmd 'npm run build'",
  "framework": "vite",
  "functions": {
    "api/index.py": {
      "excludeFiles": "**"
    }
  },
  "excludeFiles": ["api/app/**", "api/run_server.py"]
}
