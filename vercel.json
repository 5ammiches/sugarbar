{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx convex deploy --cmd 'npm run build'",
  "framework": "vite",
  "functions": {
    "api/index.py": {
      "runtime": "python3.12",
      "excludeFiles": "**"
    }
  }
}
